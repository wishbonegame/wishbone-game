import React from "react";
import ReactDOM from "react-dom/client";
import WishboneGame from "./WishboneGame";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <WishboneGame />
  </React.StrictMode>
);